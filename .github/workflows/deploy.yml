
name: Deploy & Build
on: push
  # push:
    # branches: [ main ]

jobs:
  deploy:
    name: Deploy & build code
    runs-on: self-hosted

    steps:      
      - name: Checkout repository
        uses: actions/checkout@v3
      
      - name: Copy files over to home
        run: rsync -r . ~/chatbot-prod --exclude .git

      - name: Install dependencies
        run: cd ~/chatbot-prod && npm i

      - name: Build code
        run: cd ~/chatbot-prod && pm run build

      - name: Prune dev-Dependencies
        run: cd ~/chatbot-prod && npm prune --omit=dev
   
      - name: Run the code
        run: pm2 start ~/chatbot-prod/build/src/index.js --name chatbot-prod

        
